Вместо `<текста в угловых скобках>` подставить нужный код
```
<Объявление переменной логического типа с названием isRainy равной true>
if (isRainy) <Вывод на экран "Нужен зонт"> else <Вывод на экран "Зонт не нужен">
<Установка значения переменной isRainy в false>
val text = <если isRainy - true - "Нужен зонт", иначе - "Зонт не нужен">
<Вывод константы text на экран>

<Объявление перменной dayNumber, равной 4>
val dayName = when (dayNumber) {
    1 -> "Понедельник"
    <Остальные возможные дни недели>
    else -> "Несуществующий день"
}
<Вывод названия дня недели>
<Установить isRainy в true если день недели - четверг>
<Вывести isRainy на экран>

<Увеличение dayNumber на 1>
val isRainbowVisible = <если предыдущий день - четверг - true, иначе - false>
<Если isRainbowVisible - вывести "Радуга" иначе вывести "=(">
```